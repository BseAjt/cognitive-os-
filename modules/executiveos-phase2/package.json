{
  "name": "executiveos-phase2",
  "version": "0.2.0",
  "private": true,
  "description": "ExecutiveOS Phase 2 - task engine, agent contracts, cognitive graph and PostgreSQL-ready persistence",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/server.js && node --check public/app.js"
  },
  "engines": { "node": ">=20" },
  "optionalDependencies": { "pg": "^8.13.1" }
}
